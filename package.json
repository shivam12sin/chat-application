{
  "dependencies": {
    "artillery": "^2.0.27",
    "axios": "^1.13.2",
    "pg": "^8.16.3",
    "puppeteer": "^24.31.0",
    "socket.io-client": "^4.8.1",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.3.0",
    "@commitlint/config-conventional": "^20.3.0",
    "cross-env": "^10.1.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7"
  },
  "scripts": {
    "prepare": "husky",
    "lint:backend": "cd backend && npx eslint src --fix",
    "lint:frontend": "cd frontend && npx eslint src --fix",
    "typecheck:backend": "cd backend && npx tsc --noEmit",
    "typecheck:frontend": "cd frontend && npx tsc --noEmit"
  },
  "lint-staged": {
    "backend/src/**/*.ts": [
      "cross-env ESLINT_USE_FLAT_CONFIG=true npx --prefix backend eslint --config backend/eslint.config.mjs --fix",
      "npx --prefix backend prettier --write"
    ],
    "frontend/src/**/*.{ts,tsx}": [
      "npx --prefix frontend eslint --config frontend/.eslintrc.json --fix",
      "npx --prefix frontend prettier --write"
    ]
  }
}
